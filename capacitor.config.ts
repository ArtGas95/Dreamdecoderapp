
import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.dreamdecoder',
  appName: 'DreamDecoder',
  webDir: 'public',
  bundledWebRuntime: false
};

export default config;
